{
  "name": "@manzano/component-utils",
  "version": "1.0.0",
  "main": "index.js",
  "config": {
    "distFolder": "dist"
  },
  "scripts": {
    "build": "rm -rf $npm_package_config_distFolder && tsc && cp package.json $npm_package_config_distFolder",
    "send": "npm publish $npm_package_config_distFolder --access public",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "ts-util-is": "1.1.3"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "devDependencies": {
    "@types/react": "16.9.19",
    "react": "16.12.0",
    "typescript": "3.7.5"
  }
}
